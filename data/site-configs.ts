import { SiteConfigs } from "../src/types/index";

const configs: SiteConfigs = {
  borderColors: {
    "getting-started": "blue",
    showcase: "green",
    community: "red",
    about: "yellow",
    calendar: "orange"
  }
};

export default configs;
